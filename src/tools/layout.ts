import type { VueKitLayout, VueKitLayoutsManagerSettings } from '../types';

export const defineVueKitLayout = (layout: VueKitLayout) => layout;

export const defineVueKitLayouts = (layouts: VueKitLayout[]) => layouts;

export const defineVueKitLayoutsSettings = (
  layoutsSettings: VueKitLayoutsManagerSettings,
) => layoutsSettings;
